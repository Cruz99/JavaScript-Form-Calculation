/*Assigning variables for each cup (menu has to dynamically check/uncheck checkboxes when there are more then 1 selected*/var cupSmall = document.getElementById("cup-small");var cupMedium = document.getElementById("cup-medium");var cupLarge = document.getElementById("cup-large");var cupXlarge = document.getElementById("cup-xlarge");var newArr =  new Array(cupSmall,cupMedium,cupLarge,cupXlarge); //Creating an array to loop through the optionsvar priceArr = new Array (0, 0, 0, 0); // Initializing an array with "0" for each section/* Initializing "onclick" events. addEventListeners do not trigger checkboxes. I had to use .onclick method in order to set up onclick event Each method triggers individual checkbox within "cup" secion.*/cupSmall.onclick = function() { checkCup("cup-small");};cupMedium.onclick = function() { checkCup("cup-medium");};cupLarge.onclick = function() { checkCup("cup-large");};cupXlarge.onclick = function() { checkCup("cup-xlarge");};function test(num){  window.alert("bla " + num);}/*This method checks if the cup is selected and dynamically adds/deduct the price value in priceArr[0]*/function checkCup(id){ var id = document.getElementById(id); //id of "cup section" that needs is checked/unchecked var test = id.checked;  //boolean variable  var price = id.value;  // value of id  if(test == true){   //if id is checked then loop through newArr[] to "uncheck" other checkboxes   priceArr[0] = price; // setting up the price of "cup" section    for (var i = 0; i< newArr.length;i++){     if (newArr[i].value != id.value){      newArr[i].checked = false;     }    }  }  if(test == false) {  //if selected id is "unchecked" then deduct the value within "cup array"   priceArr[0] -= priceArr[0];  }  return priceArr[0]; // returns total peice of "cup"}/* This method valides if no more then 2 cups are selected at the time. It also checks the value to "cup" checkbox. If true - add price, if false -  value of cup is deducted.*/function setPricesExceptCups(){  var upgrade = document.getElementsByClassName("class-upgrade"); //selecting all options from "upgrade" section  var addOn = document.getElementsByClassName("class-addon"); //selecting all options from "addon" section  var supplier = document.getElementsByClassName("class-supplier"); //selecting all options from "supplier" section      /*At first, set up all values within priceArr to 0*/  for (var x= 0; x<priceArr.length; x++){   priceArr[x] = 0;  }  /* The below each FOR loop loops though assigned section to validate how many inputs are true (checked). If checked then accumulate the  total value in an priceArr accoringly*/  var sum = 0;  for(var i = 0; i< upgrade.length; i++){   if(upgrade(i).checked  == true){    priceArr[1] += parseFloat(upgrade(i).value);   }  }  for(var i = 0; i< addOn.length; i++){   if(addOn(i).checked  == true){    priceArr[2] += parseFloat(upgrade(i).value);   }  }  for(var i = 0; i< supplier.length; i++){   if(supplier(i).checked  == true){    priceArr[3] += parseFloat(upgrade(i).value);   }     }}//Calculating price based on priceArr. Print it to html id costID//If no cup is selected, throw error messagefunction setPrice(){  setPricesExceptCups(); // calling function - setting up the total values  var priceLabel = document.getElementById("costTot"); // fnding and id  var check = checkIfCupSelected(); // boolean value - checks if "CUP" is selected  var sum = 0; //total sum setted to 0    if (check === true){ // iF cup is selected calculate total value    for(var i = 0; i < priceArr.length;i++){    sum += priceArr[i];   }    priceLabel.innerHTML = "€ " + sum.toFixed(2);  }  if (check === false){ // otherwise throw an error   window.alert("You must select cup size");  }    }// Checking is cup is selected - returns boolean//  If cup is selected assignes the value to array [0] (cup array)function checkIfCupSelected(){  for (var i = 0; i< newArr.length;i++){   if(newArr[i].checked == true){    priceArr[0] = parseFloat(newArr[i].value);    return true;   }  }  return false;}function validateForm(){  var myForm = document.forms["myForm"];  window.alert(myForm.elements.item(1).value);  if (checkIfCupSelected() ===true){      return true;    }  window.alert("make sure to select cup before submmiting");  return false;}
